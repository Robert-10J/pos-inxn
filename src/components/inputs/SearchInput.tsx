import { Input } from '@material-tailwind/react'

const SearchInput = () => {
  return (
    <Input label="Buscar" placeholder="Buscar" crossOrigin={''} color='purple' size='lg' />
  )
}

export default SearchInput